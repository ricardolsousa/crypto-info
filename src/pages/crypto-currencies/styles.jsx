import { createStyles } from "@mui/styles";

export const styles = (theme) =>
  createStyles({
    title: {
      marginTop: "0px",
      marginBottom: "16px",
      color: theme.palette.primary.main,
    },
  });
